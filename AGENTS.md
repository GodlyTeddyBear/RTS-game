# AGENTS

## Entry Point
- See `CLAUDE.md` first for the top-level workflow and navigation.

## Source Of Truth
- Treat the `.claude/` directory as the primary source for project markdown guidance.
- Before making plans or edits, review relevant `.md` files inside `.claude/`.
- If root-level docs conflict with `.claude` docs, follow `.claude`.

## Working Rule
- Prefer existing instructions in `.claude` over creating new conventions.
- Keep changes aligned with documented architecture, style, and workflows found there.
