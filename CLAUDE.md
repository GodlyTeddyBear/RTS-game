# CLAUDE.md

This project has migrated its active agent guidance to Codex.

Use `AGENTS.md` as the source of truth. Project guidance, documents, and command templates now live under `.codex/`.

The `.claude/` folder is intentionally kept during the transition period for historical reference only.
