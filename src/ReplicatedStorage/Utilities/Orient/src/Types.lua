--!strict

--[=[
    @class OrientTypes
    Shared type aliases used by the `Orient` package surface.
    @server
    @client
]=]

--[=[
    @type TGridSize number | Vector3
    @within OrientTypes
    Shared grid-size input used by snapping helpers.
]=]
export type TGridSize = number | Vector3

return nil
