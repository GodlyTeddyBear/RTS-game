--!strict

--[[
	RankSpecs — Eligibility rules for rank-related operations.

	Auto-promotion replaces manual rank exams, so this file now only retains
	any shared candidate types needed by other domain logic.
]]

return table.freeze({})
